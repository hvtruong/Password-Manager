import Agent from 'undici-types/agenttypes/agent'
import buildConneundici-types/connectorundici-types/connector'undici-types/client Client from 'undici-typesundici-types/dispatcherort Dispatcher from 'undici-types/dispaundici-types/headermport { IncomingHttpundici-types/pools } from 'undici-types/header'
import Pool from 'undici-types/pool'

export default ProxyAgent

declare class ProxyAgent extends Dispatcher {
  constructor(options: ProxyAgent.Options | string)

  dispatch(options: Agent.DispatchOptions, handler: Dispatcher.DispatchHandlers): boolean;
  close(): Promise<void>;
}

declare namespace ProxyAgent {
  export interface Options extends Agent.Options {
    uri: string;
    /**
     * @deprecated use opts.token
     */
    auth?: string;
    token?: string;
    headers?: IncomingHttpHeaders;
    requestTls?: buildConnector.BuildOptions;
    proxyTls?: buildConnector.BuildOptions;
    clientFactory?(origin: URL, opts: object): Dispatcher;
  }
}
